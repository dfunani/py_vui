"""py_vui — document model schema version."""

from typing import Literal

SchemaVersion = Literal["1"]
AdapterID = Literal["pyside6"]

SCHEMA_VERSION: SchemaVersion = "1"
ADAPTER_ID: AdapterID = "pyside6"
